The disabled-NSCD stub must accept the same service name as the real function.

--- a/lib/nscd.h
+++ b/lib/nscd.h
@@ -8,7 +8,7 @@
 extern int nscd_flush_cache (const char *service);
 #else
 static inline int
-nscd_flush_cache(MAYBE_UNUSED int _1)
+nscd_flush_cache(MAYBE_UNUSED const char *service)
 {
 	return 0;
 }
