This Linux-only fork is installed by bp's app-crypto/pass ebuild.
There is no separate Makefile or source download.

From the bp repository root, test it without changing the host installation:

    BP_TEST_USE=test scripts/test-ebuild app-crypto/pass/pass-20260510-r1.ebuild

See ../README.md for dependencies, optional features, provenance, and test scope.
