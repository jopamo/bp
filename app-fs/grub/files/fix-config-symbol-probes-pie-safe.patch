diff --git a/acinclude.m4 b/acinclude.m4
--- a/acinclude.m4
+++ b/acinclude.m4
@@ -232,8 +232,8 @@ AC_CACHE_VAL(grub_cv_check_uscore_uscore_bss_start_symbol,
 void __main (void);
 void __main (void) {}
 int main (void);
-]],
-		[[asm ("incl __bss_start")]])],
+extern char __bss_start;]],
+		[[volatile char *p = &__bss_start; return p == 0;]])],
 		[grub_cv_check_uscore_uscore_bss_start_symbol=yes],
 		[grub_cv_check_uscore_uscore_bss_start_symbol=no])])
 
@@ -245,8 +245,9 @@ AC_MSG_CHECKING([if edata is defined by the compiler])
 asm (".globl start; start:");
 void __main (void);
 void __main (void) {}
-int main (void);]],
-		[[asm ("incl edata")]])],
+int main (void);
+extern char edata;]],
+		[[volatile char *p = &edata; return p == 0;]])],
 		[grub_cv_check_edata_symbol=yes],
 		[grub_cv_check_edata_symbol=no])])
 
@@ -258,8 +259,9 @@ AC_MSG_CHECKING([if _edata is defined by the compiler])
 asm (".globl start; start:");
 void __main (void);
 void __main (void) {}
-int main (void);]],
-		[[asm ("incl _edata")]])],
+int main (void);
+extern char _edata;]],
+		[[volatile char *p = &_edata; return p == 0;]])],
 		[grub_cv_check_uscore_edata_symbol=yes],
 		[grub_cv_check_uscore_edata_symbol=no])])
 
@@ -286,8 +288,9 @@ AC_MSG_CHECKING([if end is defined by the compiler])
 asm (".globl start; start:");
 void __main (void);
 void __main (void) {}
-int main (void);]],
-		[[asm ("incl end")]])],
+int main (void);
+extern char end;]],
+		[[volatile char *p = &end; return p == 0;]])],
 		[grub_cv_check_end_symbol=yes],
 		[grub_cv_check_end_symbol=no])])
 
@@ -299,8 +302,9 @@ AC_MSG_CHECKING([if _end is defined by the compiler])
 asm (".globl start; start:");
 void __main (void);
 void __main (void) {}
-int main (void);]],
-		[[asm ("incl _end")]])],
+int main (void);
+extern char _end;]],
+		[[volatile char *p = &_end; return p == 0;]])],
 		[grub_cv_check_uscore_end_symbol=yes],
 		[grub_cv_check_uscore_end_symbol=no])])
 
