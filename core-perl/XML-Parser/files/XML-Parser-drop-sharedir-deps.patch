--- XML-Parser-a0cecfc4deb709703c6e3472138001d003abf7b3/Expat/Expat.pm
+++ genpatch/Expat/Expat.pm
@@ -13,17 +13,14 @@
 our ( %Encoding_Table, @Encoding_Path );
 
 use File::Spec ();
-use File::ShareDir ();
-
 %Encoding_Table = ();
 
-my $_share_dir;
-eval { $_share_dir = File::ShareDir::dist_dir('XML-Parser') };
-
 @Encoding_Path = (
-    ( defined $_share_dir && -d $_share_dir ? ($_share_dir) : () ),
-    grep( -d $_,
-        map( File::Spec->catdir( $_, qw(XML Parser Encodings) ), @INC ) ),
+    grep(
+        -d $_,
+        File::Spec->catdir( File::Spec->curdir, 'share' ),
+        map( File::Spec->catdir( $_, qw(XML Parser Encodings) ), @INC )
+    ),
     File::Spec->curdir
 );
 
--- XML-Parser-a0cecfc4deb709703c6e3472138001d003abf7b3/Makefile.PL
+++ genpatch/Makefile.PL
@@ -2,9 +2,6 @@
 use ExtUtils::MakeMaker;
 use lib './inc';
 use Devel::CheckLib;
-use File::ShareDir::Install;
-install_share dist => 'share';
-
 use Config;
 use File::Spec;
 
@@ -152,11 +149,6 @@
     $ret;
 }
 
-sub MY::postamble {
-    my $self = shift;
-    my @ret  = File::ShareDir::Install::postamble($self);
-    return join "\n", @ret;
-}
 
 WriteMakefile1(
     ABSTRACT_FROM    => 'Parser.pm',
@@ -186,13 +178,6 @@
     clean        => { FILES => 'assertlib*' },
     dist         => { COMPRESS => 'gzip', SUFFIX => '.gz' },
     VERSION_FROM => 'Parser.pm',
-    CONFIGURE_REQUIRES => {
-        'ExtUtils::MakeMaker'     => 0,
-        'File::ShareDir::Install' => '0.06',
-    },
-    PREREQ_PM => {
-        'File::ShareDir' => 0,
-    },
     $^O =~ /win/i
     ? (
         dist => {
