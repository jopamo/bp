diff --git a/lib/meson.build b/lib/meson.build
index 6bda02e..1d8eb83 100644
--- a/lib/meson.build
+++ b/lib/meson.build
@@ -25,6 +25,10 @@ else
 endif
 
 deps = [ thread_dep ]
+if get_option('musl-bsd-source').enabled()
+   deps += [ dependency('musl-bsd-source', required: true) ]
+endif
+
 if private_cfg.get('HAVE_ICONV')
    libfuse_sources += [ 'modules/iconv.c' ]
    libiconv = cc.find_library('iconv', required: false)
diff --git a/meson_options.txt b/meson_options.txt
index 9bc57d4..36fc705 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -1,3 +1,6 @@
+option('musl-bsd-source', type : 'feature', value : 'disabled',
+       description: 'Link the explicit musl-bsd source-compatibility interface into libfuse')
+
 option('disable-mtab', type : 'boolean', value : false,
        description: 'Disable and ignore usage of /etc/mtab')
 
