#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

lxqt-panel &
rm -rf ~/.cache && mkdir -p /tmp/$(whoami).cache && ln -s /tmp/$(whoami).cache ~/.cache
picom -b --glx-no-rebind-pixmap --unredir-if-possible --glx-no-stencil --vsync
hsetroot -solid '#000000' &
alsactl --file /var/lib/alsa/state restore
